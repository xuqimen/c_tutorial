/*
 * Quiz: Conditionals in C
 * Instructions: Fill in the blanks to make the code work as described.
 */
#include <stdio.h>

int main() {
    int score = 85;
    // 1. Fill in the condition to check if score is greater than or equal to 60
    if (/* TODO: write a condition to check if score >= 60 */) {
        printf("Pass\n");
    } else {
        printf("Fail\n");
    }

    // 2. Fill in the blanks to print "Excellent" if score >= 90
    if (/* TODO: write a condition to check if score >= 90 */) {
        printf("Excellent\n");
    }
    return 0;
}
