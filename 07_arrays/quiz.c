/*
 * Quiz: Arrays in C
 * Instructions: Fill in the blanks to make the code work as described.
 */
#include <stdio.h>

int main() {
    // 1. Declare an array of 5 integers and assign values
    int arr[5] = { /* TODO: assign values */ };
    // 2. Print the third element of the array
    printf("Third element: %d\n", /* TODO: access the third element */);

    // 3. Fill in the loop to print all elements
    for (int i = 0; i < 5; i++) {
        printf("%d ", /* TODO: print each element */);
    }
    printf("\n");
    return 0;
}
