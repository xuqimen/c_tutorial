/*
 * Quiz: Strings in C
 * Instructions: Fill in the blanks to make the code work as described.
 */
#include <stdio.h>
#include <string.h>

int main() {
    // 1. Declare a string and assign it the value "Hello"
    char str[10] = /* TODO: assign a string value */;
    printf("String: %s\n", str);

    // 2. Fill in the code to print the length of the string
    int len = /* TODO: use strlen to get length */;
    printf("Length: %d\n", len);
    return 0;
}
