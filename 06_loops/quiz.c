/*
 * Quiz: Loops in C
 * Instructions: Fill in the blanks to make the code work as described.
 */
#include <stdio.h>

int main() {
    // 1. Fill in the loop to print numbers 1 to 5
    int i;
    for (/* TODO: initialize i; condition; increment */) {
        printf("%d ", i);
    }
    printf("\n");

    // 2. Fill in the while loop to print "Hello" 3 times
    int count = 0;
    while (/* TODO: write a condition to run loop 3 times */) {
        printf("Hello\n");
        count++;
    }
    return 0;
}
