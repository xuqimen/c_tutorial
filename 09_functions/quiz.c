/*
 * Quiz: Functions in C
 * Instructions: Fill in the blanks to make the code work as described.
 */
#include <stdio.h>

// 1. Fill in the function definition to return the sum of two integers
int sum(/* TODO: function parameters */) {
    return /* TODO: return the sum */;
}

int main() {
    int a = 3, b = 7;
    int result = sum(a, b);
    printf("Sum: %d\n", result); // Should print 10
    return 0;
}
