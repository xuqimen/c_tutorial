/*
 * Quiz: Operators in C
 * Instructions: Uncomment and fill in the blanks to make the code work as described.
 */
#include <stdio.h>

int main() {
    // 1. Fill in the value so that result is 15
    int a = 10;
    int b = 5;
    int result =  /* TODO: add a and b */
    printf("Sum: %d\n", result); // Should print 15

    // 2. Fill in the expression so that product is 50
    int product = /* TODO: write an expression to calculate the product of a and b */
    printf("Product: %d\n", product);

    // 3. Fill in the condition so that it prints "Is a greater than b? Yes" when a is greater than b
    if (/* TODO: write a condition to check if a is greater than b */) {
        printf("Is a greater than b? Yes\n");
    } else {
        printf("Is a greater than b? No\n");
    }

    return 0;
}
